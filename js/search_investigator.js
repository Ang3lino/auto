
let investigator = document.getElementById('id_insti')
let institutionByCountry = investigator.getElementsByTagName('optgroup')
let countriesCount = institutionByCountry.length
for (let i = 0; i < countriesCount; ++i) {
    for (let j = i + 1; j < countriesCount; ++j) {
        
    }
}

